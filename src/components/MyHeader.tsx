import { Layout } from "antd";

const { Header } = Layout;
const MyHeader = () => {
    return (
        <>
            <Header style={{ padding: 0, backgroundColor: "#ffffff" }} className="flex flex-row-reverse">
                <div>sdjsd</div>
            </Header>
        </>
    );
};

export default MyHeader;
